/** @type {import('postcss-load-config').Config} */
// PostCSS config for Void Motion (M02): Tailwind + Autoprefixer pipeline.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
